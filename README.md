# Bitrix DDD Reviews Component

Компонент для отзывов о ЖК с DDD архитектурой, AJAX и современным фронтендом.

## 🚀 Особенности

- **DDD Архитектура** - Domain-Driven Design подход
- **Bitrix D7** - современный Bitrix API
- **AJAX формы** - добавление без перезагрузки
- **ES6+ JavaScript** - современный синтаксис
- **SASS/SCSS** - препроцессор стилей
- **Адаптивная верстка** - Flex/Grid layout
- **Оптимистичные updates** - улучшенный UX

## 🏗️ Архитектура

```
Reviews (Агрегат)
├── Review (Entity)
├── Value Objects (Rating, Author, Text, ReviewId)
├── Repository Pattern
└── Service Layer
```

## 📦 Установка

1. Скопируйте компонент в `/local/components/custom/reviews.list/`
2. Создайте таблицу в БД:
```sql
CREATE TABLE custom_reviews (
    ID INT AUTO_INCREMENT PRIMARY KEY,
    AUTHOR VARCHAR(100) NOT NULL,
    TEXT TEXT NOT NULL,
    RATING INT CHECK (RATING BETWEEN 1 AND 5),
    CREATED_AT DATETIME NOT NULL,
    USER_ID INT NULL
);
```
3. Добавьте на страницу:

```php
<?php
$APPLICATION->IncludeComponent(
    "custom:reviews.list",
    ".default",
    [
        "COUNT" => 10,
        "CACHE_TIME" => 300
    ]
);
?>
```
## 📁 Структура

```
/local/components/custom/reviews.list/
├── class.php                  # Основной класс компонента
├── .description.php           # Описание для админки
├── .parameters.php            # Параметры компонента
├── ajax.php                   # AJAX обработчик
├── lib/                       # DDD слои
│   ├── Domain/
│   │   ├── Entity/Review.php
│   │   ├── ValueObject/
│   │   └── Repository/
│   ├── Infrastructure/
│   └── Application/
├── templates/
│   └── .default/
│       ├── template.php       # Шаблон
│       ├── script.js          # ES6+ JavaScript
│       ├── style.scss         # SASS стили
│       └── style.css          # Скомпилированные стили
└── README.md
```

## 🎨 Интерфейс

- Карточки отзывов с рейтингом в виде звезд
- Адаптивная сетка
- Hover-эффекты
- Валидация в реальном времени
- Уведомления об успехе/ошибке

## ⚡ Производительность

- Кеширование запросов
- Оптимистичные UI updates
- Ленивая загрузка ресурсов
- Минимизация перерисовок DOM

---

Разработано в рамках тестового задания. Демонстрирует владение современными подходами к разработке на Bitrix.
